interface:
  display_name: "OpenClaw Manager"
  short_description: "Control sessions, runs, checkpoints, and focus"
  default_prompt: "Use $openclaw-manager to inspect sessions, resume a stalled task, or export a share snapshot. When relevant, surface the local session console URL at /ui."

policy:
  allow_implicit_invocation: true
